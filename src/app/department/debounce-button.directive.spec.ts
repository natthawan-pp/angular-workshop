import { DebounceButtonDirective } from './debounce-button.directive';

describe('DebounceButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceButtonDirective();
    expect(directive).toBeTruthy();
  });
});
