import { BudgetOverloadDirective } from './budget-overload.directive';

describe('BudgetOverloadDirective', () => {
  it('should create an instance', () => {
    const directive = new BudgetOverloadDirective();
    expect(directive).toBeTruthy();
  });
});
