import { AutoSearchDirective } from './auto-search.directive';

describe('AutoSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoSearchDirective();
    expect(directive).toBeTruthy();
  });
});
